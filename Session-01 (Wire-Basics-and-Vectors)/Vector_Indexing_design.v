/// Problem 2: Vector Indexing

module vector_indexing(
  input wire [2:0] in_vector,
  output wire [2:0] out_vector,
  output wire out2, out1, out0
);
  // Approach: Extract individual bits from the input vector.
  // Steps:
  // 1. Declare input and output ports.
  // 2. Assign the entire vector to an output.
  // 3. Use indexing to extract specific bits.
endmodule
