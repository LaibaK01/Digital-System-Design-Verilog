/// Problem 1: Multiple Wire Connections

module wire_connections(
  input wire a, b, c,
  output wire w, x, y, z
);
  // Approach: Assign inputs to outputs directly.
  // Steps:
  // 1. Declare input and output ports.
  // 2. Use continuous assignment to connect inputs to outputs.
endmodule
